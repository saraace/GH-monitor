fragment PR on PullRequest {
  id
  title
  state
  isDraft
  merged
  url
  createdAt
  changedFiles
  lastEditedAt
  additions
  reviewDecision
  number
  statusCheckRollup {
    id
    state
  }
  author {
    ... on User {
      id
      avatarUrl(size: 64)
      login
    }
  }
}
